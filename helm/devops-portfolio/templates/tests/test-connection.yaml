apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-portfolio.fullname" . }}-test-connection"
  labels:
    {{- include "devops-portfolio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-portfolio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
