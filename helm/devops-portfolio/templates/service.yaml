apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-portfolio.fullname" . }}
  labels:
    {{- include "devops-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-portfolio.selectorLabels" . | nindent 4 }}
